pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/milestone' }
		maven { url 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
	}
}
rootProject.name = 'ops'
include 'ops-api'
include 'ops-admin'
include 'ops-core'
include 'ops-swagger'

